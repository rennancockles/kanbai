[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kanbai"
version = "0.4.0"
description = "A file-based Kanban board for Claude Code and other AI coding harnesses"
authors = [
    {name = "Rennan Cockles", email = "rennan.cockles@experian.com"},
]
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
keywords = ["kanban", "claude", "claude-code", "ai", "agent", "task-management"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development",
    "Topic :: Utilities",
]
dependencies = [
    "typer>=0.12",
    "pydantic>=2.6",
    "pyyaml>=6.0",
    "rich>=13.0",
    "httpx2[socks]>=2.13",
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
ui = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "jinja2>=3.1",
    "watchfiles>=0.21",
    "python-multipart>=0.0.9",
    "desktop-notifier>=6.0",
]
# Lets the python-semantic-release GitHub Action's build_command install a pinned uv — its
# Docker environment doesn't ship one, unlike the CI/release runners (astral-sh/setup-uv).
build = ["uv>=0.7"]

[dependency-groups]
dev = [
    "ruff>=0.16",
    "mypy>=2.3",
    "ty>=0.0.75",
    "pytest>=9.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "pytest-rerunfailures>=16.6",
    "pytest-xdist>=3.8.0",
    "types-pyyaml>=6.0",
    "python-semantic-release>=10.7",
]
docs = [
    "mkdocs-material>=9.5",
]

[project.scripts]
kanbai = "kanbai.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["kanbai"]
# Ship the web UI's Jinja2 templates and static assets (CSS, vendored htmx) in the wheel.
artifacts = [
    "kanbai/web/templates/**/*.html",
    "kanbai/web/static/**/*",
]


[tool.ruff]
line-length = 100
src = ["kanbai", "tests"]
indent-width = 4
respect-gitignore = true
target-version = "py310"

[tool.ruff.lint]
select = [
    "C",  # McCabe complexity
    "E",  # pycodestyle
    "W",  # pycodestyle warnings
    "F",  # Pyflakes
    "I",  # isort
    "UP",  # pyupgrade
    "PL",  # Pylint
    "B",  # flake8-bugbear
    "ASYNC",  # flake8-async
    "SIM",  # flake8-simplify
    "N",  # pep8-naming
]
ignore = [
]

# Allow fix for all enabled rules (when `--fix`) is provided.
fixable = ["ALL"]
unfixable = []

# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]  # Imported but unused
"tests/**/*" = [
    "PLR2004",  # Magic value used in comparison
    "SIM300",  # Yoda conditions are discouraged
]
"kanbai/cli.py" = [
    "B008",  # Typer's idiom is to call Option()/Argument() in parameter defaults.
    "PLR0913",  # CLI commands legitimately expose many options.
    "PLR0917",
]
"kanbai/web/app.py" = ["B008"]  # FastAPI's idiom is to call Form()/Depends() in defaults.

[tool.ruff.lint.pylint]
max-args = 7

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"

[tool.ruff.lint.isort]
combine-as-imports = true
# known-first-party = ["kanbai", "tests"]
# extra-standard-library = ["typing_extensions"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"


[tool.mypy]
python_version = "3.10"
strict = true
packages = ["kanbai"]
cache_fine_grained = true
ignore_missing_imports = true
disallow_untyped_defs = true
check_untyped_defs = true
warn_return_any = true
warn_unreachable = true
plugins = [
    "pydantic.mypy",
]


[tool.ty.src]
include = ["kanbai"]

[tool.ty.rules]
unused-ignore-comment = "ignore"
no-matching-overload = "ignore"
unused-type-ignore-comment = "ignore"

[tool.ty.environment]
python-version = "3.10"

[tool.ty.terminal]
output-format = "concise"


[tool.pytest.ini_options]
addopts = "-rfEs --strict-config --strict-markers -n auto --no-header"
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = [
    "tests",
]
markers = [
    "unit: mark a test as a unit test",
    "integration: mark a test as an integration test"
]


[tool.coverage.run]
source = ["kanbai"]

[tool.coverage.report]
fail_under = 75


[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
version_variables = ["kanbai/__init__.py:__version__"]
allow_zero_version = true
commit_message = "chore(release): bumping to version {version}"
build_command = """
    python -m pip install -e '.[build]'
    uv lock --upgrade-package kanbai
    git add uv.lock
"""

[tool.semantic_release.changelog]
exclude_commit_patterns = ["chore(release): bumping to version .*"]

[tool.semantic_release.branches.main]
match = "main"

[tool.semantic_release.commit_parser_options]
allowed_tags = ["build", "chore", "ci", "docs", "feat", "fix", "perf", "refactor", "style", "test"]
minor_tags = ["feat"]
patch_tags = ["fix", "perf"]
default_bump_level = 0

[tool.semantic_release.remote]
type = "github"
